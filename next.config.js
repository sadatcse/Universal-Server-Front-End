/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com',"images.pexels.com", "img.freepik.com","i.pinimg.com"],
      },
}

module.exports = nextConfig
